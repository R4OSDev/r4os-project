GitHub-Arbeitsanleitung
======================

Zweck
-----

Diese Datei beschreibt den vollständigen GitHub-Arbeitsablauf für den
R4OS-Workspace unter Windows und Linux. Sie ist die verbindliche Anleitung für
Agents und andere automatisierte Werkzeuge. Die Kurzfassung in AGENTS.md nennt
nur den Einstieg für Push und Pull.


Grundregeln
-----------

- Für Project, DevKit, Docs, Organisationsprofil, Contract, SDK, Libraries,
  Kernel, Recovery, Distribution und Module ausschließlich `Tools\Github.bat` unter
  Windows oder `./Tools/Github.sh` unter Linux verwenden. Beide starten die
  gemeinsame PowerShell-7-Logik in `Tools/Github.ps1`.
- Der private Package-Server ist die einzige Ausnahme. Er wird ausschließlich
  mit seinen eigenen Startern unter `Server/` verwaltet und besitzt bewusst
  kein Ziel im globalen GitHub-Starter.
- Pulls aus allen vom globalen Script verwalteten öffentlichen Repositories
  laufen anonym. Github.ps1 lädt dafür keine Credentials und deaktiviert
  interaktive Anmeldung sowie lokale Git-Credential-Helper.
- Pushes und die automatische Repository-Erstellung bleiben
  tokenauthentifiziert.
- Keine direkten Aufrufe von git push oder git pull verwenden.
- Read-only-Aufrufe wie git status, git diff, git log und git rev-parse sind
  zur Prüfung erlaubt.
- Jedes R4OS-Repository ist unabhängig. Dateien werden weiterhin nur im
  Repository ihres fachlichen Besitzers bearbeitet; beim normalen Abschluss
  ermittelt der Sammel-Push die betroffenen Repositories selbst.
- Ein Project-Push synchronisiert keine Repositories unter Repositories,
  DevKit oder Docs. Diese Verzeichnisse sind eigenständige Repositories und
  im Project-Repository ausgeschlossen.
- Alle verwalteten Repositories verwenden origin und den Branch main.
- Github.ps1 erstellt keine Pull Requests und führt keine Branch-Merges oder
  Rebases aus. Es arbeitet direkt mit main.
- Zugangsdaten niemals lesen, anzeigen, protokollieren, kopieren oder in einen
  Commit aufnehmen.
- Der normale Abschluss erfolgt mit `Tools\Github.bat -Push -Changed` unter
  Windows beziehungsweise `./Tools/Github.sh -Push -Changed` unter Linux. Vor dem
  ersten Einzel-Push zeigt dieser alle verwalteten Repositoryzustaende und
  die erkannten Pushziele an. Gezielte Einzel-Pushes sind nur fuer Reparatur-
  und Sonderfaelle vorgesehen.
- Github.ps1 staged in jedem erkannten Ziel sämtliche nicht ignorierten
  Änderungen dieses Repositories, nicht nur die Dateien der aktuellen
  Aufgabe. Deshalb vorhandene Änderungen vor dem Sammel-Push vollständig
  prüfen.
- Vorhandene fremde oder noch nicht abgeschlossene Änderungen niemals
  verwerfen, zurücksetzen oder überschreiben. Wenn sie nicht mit committed
  werden dürfen, den Push auslassen und die Situation klären.
- Erfolg und Fehlschlag werden am Exitcode des Scripts gemessen.


Organisation und Branch
-----------------------

GitHub-Organisation: R4OSDev
Remote-Protokoll: HTTPS
Remote-Name: origin
Branch: main

Project, DevKit, Docs, Organisationsprofil, Contract, SDK, Libraries, Kernel,
Recovery, Distribution sowie Apps, Services, Diagnostics, Drivers, Protocols und
Subsystems werden
bei einem fehlenden Remote-Repository oeffentlich angelegt. Der Package-Server
ist die private Ausnahme, wird aber nicht vom globalen Script angelegt oder
verwaltet.
Bereits vorhandene Repositories werden durch Push oder Pull nicht in ihrer
Sichtbarkeit veraendert.

Github.ps1 akzeptiert ausschließlich ein origin, dessen URL exakt zum
ausgewählten R4OSDev-Repository gehört. Ein abweichendes origin führt zum
Abbruch und wird nicht automatisch überschrieben.


Credentials
-----------

Die lokalen Zugangsdaten liegen ausschließlich hier:

    Tools/Credentials/Github.json

Die JSON-Datei enthält intern `user` und `token`. Agents dürfen die Datei nicht
öffnen oder ihren Inhalt ausgeben. Github.ps1 lädt sie ausschließlich für
Pushes und schreibende GitHub-API-Aufrufe. Git erhält die Authentisierung nur
im Prozessumfeld; die GitHub-API erhält einen Authorization-Header.
Zugangsdaten werden nicht in der Remote-URL gespeichert.

Nach einem frischen Checkout wird ausgeführt:

    Tools\Setup.bat

Unter Linux entsprechend:

    ./Tools/Setup.sh

Setup.ps1 erzeugt den Credentials-Ordner und eine Platzhalterdatei, falls sie
noch nicht existieren. Eine vorhandene Credentials-Datei wird nicht
überschrieben. Für einen rein lesenden Workspace-Aufbau bleibt der Platzhalter
unverändert; erst vor einem Push trägt der Benutzer Benutzername und Token ein.

Der Token benoetigt Schreibzugriff auf Repository-Inhalte fuer Push. Soll der
globale GitHub-Starter ein fehlendes Repository automatisch anlegen, muss der
Token ausserdem in der Organisation R4OSDev Repositories erstellen duerfen. Eine
Berechtigung fuer Pull Requests ist fuer diesen Arbeitsablauf nicht
erforderlich. Pulls aus den öffentlichen R4OSDev-Repositories benötigen
weder Token noch GitHub-Benutzerkonto.

Die Credentials-Datei und ihr Verzeichnis sind im Project-Repository
ignoriert. Trotzdem vor einem Push prüfen, dass weder Token noch Kopien davon
in anderen Dateien oder Repositories stehen.


Interaktiver Aufruf
-------------------

Ohne Argumente startet das Script ein Menü:

    Tools\Github.bat       (Windows)
    ./Tools/Github.sh      (Linux)

Das Menü fragt zuerst Push oder Pull. Bei Push ist der Sammel-Push aller
geänderten Repositories die empfohlene erste Auswahl; alternativ kann ein
einzelnes Repository gewählt werden. Pull fragt weiterhin immer nach genau
einem Repository. Bei Apps, Services, Diagnostics, Drivers, Protocols und
Subsystems wird
für den Einzelweg zusätzlich der lokale Komponentenname abgefragt. Der
Package-Server ist nicht Teil dieses Menüs.

Ein interaktiver Push fragt nicht nach einer Commit-Beschreibung. Ohne direkt
übergebenen Text verwendet das Script die hinterlegte Standardbeschreibung des
Repositories.


Direkte Aufrufe
---------------

In den folgenden Befehlsformen steht `<Github-Starter>` unter Windows fuer
`Tools\Github.bat` und unter Linux fuer `./Tools/Github.sh`. Der verbindliche
normale Push-Aufruf lautet:

    <Github-Starter> -push -changed "Commit-Beschreibung"

Die Beschreibung ist optional. Ist sie vorhanden, verwendet jedes tatsächlich
geänderte Repository denselben Text. Ohne Beschreibung greift für jedes Ziel
dessen vorhandene Standardbeschreibung. Unveränderte Repositories werden
nicht committed und nicht gepusht.

Gezielte Einzel-Pushes bleiben für Reparatur- und Sonderfälle erhalten. Für
zentrale Repositories gilt:

    <Github-Starter> -push -ZIEL "Commit-Beschreibung"
    <Github-Starter> -pull -ZIEL

ZIEL ist eines von:

    -project
    -devkit
    -docs
    -contract
    -sdk
    -libraries
    -kernel
    -recovery
    -distribution
    -organization

Beispiele fuer beide Hosts:

    Tools\Github.bat -push -changed "Arbeitsstand abschliessen"
    Tools\Github.bat -pull -contract
    ./Tools/Github.sh -push -project "Agent-Anleitung aktualisieren"
    ./Tools/Github.sh -push -docs "Dokumentation aktualisieren"
    ./Tools/Github.sh -push -organization "Organisationsprofil aktualisieren"

Für eigenständige Komponenten steht der Komponentenname vor der optionalen
Commit-Beschreibung:

    <Github-Starter> -push -app NAME "Commit-Beschreibung"
    <Github-Starter> -pull -app NAME
    <Github-Starter> -push -service NAME "Commit-Beschreibung"
    <Github-Starter> -pull -service NAME
    <Github-Starter> -push -diagnostic NAME "Commit-Beschreibung"
    <Github-Starter> -pull -diagnostic NAME
    <Github-Starter> -push -driver NAME "Commit-Beschreibung"
    <Github-Starter> -pull -driver NAME
    <Github-Starter> -push -protocol NAME "Commit-Beschreibung"
    <Github-Starter> -pull -protocol NAME
    <Github-Starter> -push -subsystem NAME "Commit-Beschreibung"
    <Github-Starter> -pull -subsystem NAME

NAME muss dem lokalen Verzeichnisnamen entsprechen. Er darf nur Buchstaben,
Ziffern und Bindestriche enthalten und muss mit einem Buchstaben oder einer
Ziffer beginnen. Für den GitHub-Namen wird NAME kleingeschrieben.

Beispiele fuer beide Hosts:

    Tools\Github.bat -push -app Clock "Clock aktualisieren"
    Tools\Github.bat -pull -service SSHD
    Tools\Github.bat -push -diagnostic FsDiag "FsDiag korrigieren"
    ./Tools/Github.sh -pull -driver MIDI
    ./Tools/Github.sh -push -protocol JSON "JSON aktualisieren"
    ./Tools/Github.sh -pull -subsystem QBasic

Die Commit-Beschreibung ist optional. Ohne Beschreibung verwendet das Script
eine passende Standardbeschreibung. Für explizite Beschreibungen einen kurzen,
einzeiligen deutschen Text als genau ein Argument übergeben. Eingebettete
doppelte Anführungszeichen und Shell-Sonderzeichen vermeiden.

Sicherer normaler PowerShell-Aufruf für Agents:

    $commitMessage = 'Arbeitsstand abschliessen'
    $githubScript = (Get-Item -LiteralPath 'Tools/Github.ps1').FullName
    & pwsh -NoLogo -NoProfile -File $githubScript -push -changed $commitMessage
    exit $LASTEXITCODE


Repository-Zuordnung
--------------------

Alle lokalen Angaben sind relativ zum Workspace-Root. Der Punkt `.` bezeichnet
das Project-Repository selbst.

Project
    Ziel: -project
    Lokal: .
    GitHub: R4OSDev/r4os-project
    Inhalt: Workspace-Steuerung wie AGENTS.md, Agents, Tools und Root-Dateien.
    DevKit, Docs, Artifacts und Repositories werden nicht mit eingeschlossen.

DevKit
    Ziel: -devkit
    Lokal: DevKit/
    GitHub: R4OSDev/r4os-devkit
    Inhalt: Setup und DevKit-Struktur. Installierte Toolchains, QEMU, Limine,
    SDK-Kopien und Hosttool-Binaries sind durch die DevKit-.gitignore
    ausgeschlossen.

Docs
    Ziel: -docs
    Lokal: Docs/
    GitHub: R4OSDev/r4os-docs
    Inhalt: projektübergreifende Dokumentation und Inventare.

Server
    Kein Ziel im globalen GitHub-Starter
    Lokal: Server/
    GitHub: R4OSDev/r4os-package-server
    Sichtbarkeit: privat
    Inhalt: Website, Update- und Paketdienst, Deployment, Betriebsreferenz
    sowie die eigenen lokalen Push-/Pull-Starter.
    Push und Pull: eigene Starter des Server-Repositories verwenden

Contract
    Ziel: -contract
    Lokal: Repositories/Contract/
    GitHub: R4OSDev/r4os-contract

SDK
    Ziel: -sdk
    Lokal: Repositories/SDK/
    GitHub: R4OSDev/r4os-sdk

Libraries
    Ziel: -libraries
    Lokal: Repositories/Libraries/
    GitHub: R4OSDev/r4os-libraries

Kernel
    Ziel: -kernel
    Lokal: Repositories/Kernel/
    GitHub: R4OSDev/r4os-kernel

Recovery
    Ziel: -recovery
    Lokal: Repositories/Recovery/
    GitHub: R4OSDev/r4os-recovery
    Inhalt: Eigenstaendiges Recovery-Monorepo mit separatem Kernel,
    festgehaltenem Plattformvertrag, Laufzeitmodulen und Recovery-Werkzeugen.
    Bootfaehige BIOS-/UEFI-Recovery mit RAM-Laufzeit, Menue, Terminal,
    SSH/FTP und R4PART. Installations- und Updatewege folgen der Roadmap.

Distribution
    Ziel: -distribution
    Lokal: Repositories/Distribution/
    GitHub: R4OSDev/r4os-distribution

Organisationsprofil
    Ziel: -organization
    Lokal: Repositories/Organization/
    GitHub: R4OSDev/.github
    Sichtbarkeit: öffentlich
    Inhalt: Öffentliche GitHub-Organisationsseite unter profile/README.md und
    organisationsweite Community-Metadaten.

Apps
    Ziel: -app NAME
    Lokal: Repositories/Apps/NAME/
    GitHub: R4OSDev/r4os-app-<name-klein>

Services
    Ziel: -service NAME
    Lokal: Repositories/Services/NAME/
    GitHub: R4OSDev/r4os-service-<name-klein>

Diagnostics
    Ziel: -diagnostic NAME
    Lokal: Repositories/Diagnostics/NAME/
    GitHub: R4OSDev/r4os-diagnostic-<name-klein>

Drivers
    Ziel: -driver NAME
    Lokal: Repositories/Drivers/NAME/
    GitHub: R4OSDev/r4os-driver-<name-klein>

Protocols
    Ziel: -protocol NAME
    Lokal: Repositories/Protocols/NAME/
    GitHub: R4OSDev/r4os-protocol-<name-klein>

Subsystems
    Ziel: -subsystem NAME
    Lokal: Repositories/Subsystems/NAME/
    GitHub: R4OSDev/r4os-subsystem-<name-klein>

Agents gehört zum Project-Repository. Änderungen unter Docs gehören dagegen
zum Docs-Repository. Diese Besitzgrenzen bleiben beim Bearbeiten verbindlich;
der Sammel-Push erkennt die geänderten Repositorywurzeln anschließend ohne
manuelle Zielauswahl. Einen Sammel-Pull gibt es bewusst nicht.


Was der Sammel-Push ausführt
----------------------------

`Github.ps1 -Push -Changed` verwendet intern `Tools/GithubChanged.ps1`, aber
jeder schreibende Schritt wird weiterhin durch den vorhandenen Einzel-Push von
Github.ps1 ausgeführt. Credentials, Repository-Erstellung, Staging, Commit,
Remoteprüfung und Pushlogik werden dadurch nicht dupliziert.

Die Erkennung besitzt keinen separaten Repository-Index. Sie verwendet diese
feste Workspace-Struktur:

1. Contract, SDK, Libraries und Kernel.
2. Direkte Projektverzeichnisse unter Apps, Services, Diagnostics, Drivers,
   Protocols und Subsystems, innerhalb jeder Rolle alphabetisch.
3. Recovery, Distribution, Docs, DevKit, Organisationsprofil und zuletzt Project.

Server ist vollständig ausgeschlossen. Unterrepositories und installierte
Fremdquellen in DevKit, insbesondere QEMU, Limine und Zig, werden nicht
rekursiv gesucht und können deshalb kein Sammel-Pushziel werden. Ein neues
gültiges Komponentenverzeichnis mit `module.R4MF` in einer der sechs Rollen
wird ohne zusätzlichen Indexeintrag erkannt.

Vor dem ersten Push prüft und zeigt das Script für jedes erkannte Ziel:

- Repository und lokaler Pfad,
- Arbeitsbaumstatus einschließlich neuer und gelöschter Dateien,
- aktuellen Branch,
- Zuordnung von origin zum erwarteten R4OSDev-Remote,
- Anzahl lokal noch nicht auf origin/main vorhandener Commits.

Ein Ziel wird verarbeitet, wenn sein Arbeitsbaum Änderungen enthält oder
lokale Commits noch nicht veröffentlicht sind. Fehlende origin-Einträge und
noch nicht initialisierte gültige Komponenten darf der vorhandene Einzelweg
anlegen. Ein abweichendes origin oder ein Branch ungleich main wird nicht
korrigiert, sondern als Fehler für genau dieses Repository gemeldet.

Fehler eines Ziels verhindern nicht die Verarbeitung der übrigen erkannten
Änderungen. Am Ende werden gepushte, unverändert übersprungene und
fehlgeschlagene Repositories getrennt aufgelistet. Danach prüft das Script alle
verwalteten Ziele erneut. Exitcode 0 wird nur geliefert, wenn kein Fehler und
keine pushbare Änderung beziehungsweise kein unveröffentlichter Commit übrig
ist.

Da jeder verwaltete Arbeitsbaum vor und nach der Verarbeitung geprüft wird,
kann auch ein Lauf ohne Änderungen einige zehn Sekunden dauern. Nicht vor der
Abschlussliste und dem endgültigen Exitcode abbrechen.


Was ein Einzel-Push ausführt
----------------------------

Github.ps1 führt für das gewählte Repository in dieser Reihenfolge aus:

1. Credentials laden und Platzhalterwerte ablehnen.
2. Lokales Repository prüfen oder, außer beim Project-Root, bei Bedarf mit
   main initialisieren.
3. Sicherstellen, dass der aktuell ausgecheckte Branch main ist.
4. Pruefen, ob das GitHub-Repository existiert; ein fehlendes, unterstuetztes
   Ziel bei ausreichender Berechtigung mit seiner festgelegten Sichtbarkeit
   anlegen.
5. origin ergänzen oder seine exakte URL prüfen.
6. Mit git add -A alle Änderungen des gewählten Repositories stagen.
7. Bei einem staged Unterschied einen Commit erzeugen. Fehlen lokale
   Git-Identitätswerte, werden GitHub-Benutzername und die zugehörige
   users.noreply.github.com-Adresse repositorylokal gesetzt.
8. main mit Upstream origin/main pushen.

git add -A umfasst neue Dateien, Änderungen und Löschungen. Die jeweilige
.gitignore wird berücksichtigt. Bereits getrackte Dateien werden durch einen
späteren Ignore-Eintrag allein jedoch nicht automatisch ungetrackt. Wenn keine
neuen Änderungen vorhanden sind, wird kein leerer Commit erzeugt; ein noch
nicht veröffentlichter vorhandener main-Stand wird trotzdem gepusht.

Alle vom globalen Script unterstützten Ziele werden öffentlich angelegt. Das
private Package-Server-Repository wird von seinen eigenen Scripts nur
synchronisiert und niemals automatisch angelegt. Eine spätere Änderung der
Sichtbarkeit ist ein eigener administrativer Vorgang.


Was ein Pull ausführt
---------------------

Github.ps1 führt für das gewählte Repository in dieser Reihenfolge aus:

1. Lokales Repository prüfen oder, außer beim Project-Root, bei Bedarf mit
   main initialisieren.
2. Sicherstellen, dass der aktuell ausgecheckte Branch main ist.
3. origin ergänzen oder seine exakte öffentliche HTTPS-URL prüfen.
4. Interaktive Anmeldung und lokale Git-Credential-Helper deaktivieren.
5. git pull --ff-only origin main anonym ausführen.

Der Pull erlaubt ausschließlich Fast-Forward. Das Script führt keinen Merge,
keinen Rebase, keinen Reset, kein Clean und kein automatisches Stash aus. Bei
lokalen Konflikten oder auseinander gelaufenen Branches bricht es ab. Diesen
Abbruch nicht mit Force, Reset oder dem Verwerfen lokaler Änderungen umgehen,
sondern zuerst Status und Historie prüfen.

Ein Pull aktualisiert nur das ausgewählte Repository. Der Workspace-Build und
die einzelnen Buildstarter führen ebenfalls keine Git-Updates aus. Benötigte
Contract-, SDK-, Library-, Kernel-, Modul- und Distribution-Repositories
müssen deshalb jeweils ausdrücklich aktualisiert werden.


Arbeitsablauf für Änderungen
----------------------------

1. Eigentümer der zu ändernden Dateien bestimmen und die fachlichen
   Repositorygrenzen beim Bearbeiten einhalten. Die spätere manuelle Auswahl
   eines Pushziels entfällt.
2. Vorhandenen Zustand lesen:

       git -C "<Repository-Pfad>" status --short
       git -C "<Repository-Pfad>" diff

3. Vorhandene Benutzeränderungen erhalten und nur die beauftragten Dateien
   bearbeiten.
4. Relevante Builds, Tests und Dokumentationsprüfungen ausführen.
5. Vor dem Abschluss die Status- und Diffausgaben aller berührten
   Repositories sowie gegebenenfalls Whitespace prüfen:

       git -C "<Repository-Pfad>" status --short
       git -C "<Repository-Pfad>" diff --check

6. Den Sammel-Push mit einer klaren gemeinsamen Commit-Beschreibung aufrufen:

       Tools\Github.bat -push -changed "Arbeitsstand abschliessen"   (Windows)
       ./Tools/Github.sh -push -changed "Arbeitsstand abschliessen"  (Linux)

7. Seine Vorprüfung und Abschlusslisten vollständig lesen und den Exitcode
   prüfen.
8. Bei Exitcode ungleich 0 die genannten Repositories gezielt untersuchen und
   erst nach der Korrektur den Sammel-Push erneut ausführen. Gezielte
   Einzel-Pushes nur verwenden, wenn die Reparatur sie ausdrücklich verlangt.
9. Nach erfolgreichem Push meldet die eingebaute Abschlussprüfung keine
   verbleibenden Änderungen oder unveröffentlichten Commits. Bei Bedarf kann
   zusätzlich gezielt geprüft werden:

       git -C "<Repository-Pfad>" status --short
       git -C "<Repository-Pfad>" rev-parse HEAD
       git -C "<Repository-Pfad>" rev-parse origin/main

Die feste Sammelreihenfolge veröffentlicht abhängige Wahrheiten vor ihren
Verbrauchern und die Workspace-Steuerung zuletzt. Ein Fehlschlag macht den
Vorgang nicht atomar rückgängig; bereits erfolgreiche Repositories bleiben
veröffentlicht und werden beim nächsten Lauf als unverändert übersprungen.


Erstaufbau eines Workspace
--------------------------

Github.ps1 kann den Project-Root nicht selbst initialisieren, weil das Script
erst aus dem vorhandenen r4os-project-Checkout verfügbar ist. Sobald dieser
Checkout im frei gewaehlten Workspace-Root existiert:

1. Das öffentliche Repository R4OSDev/r4os-project ohne Credentials nach
   `<Workspace-Root>` klonen und in dieses Verzeichnis wechseln.
2. Unter Windows `Tools\Setup.bat`, unter Linux `./Tools/Setup.sh` ausfuehren.
3. DevKit mit dem passenden GitHub-Starter und `-pull -devkit` anonym beziehen.
4. Docs, Contract, SDK, Libraries, Kernel, Recovery, Distribution und bei Bedarf das
   Organisationsprofil mit ihren jeweiligen Pull-Zielen beziehen.
5. Den privaten Package-Server bei Bedarf separat klonen und danach mit
   dessen eigenem Pull-Starter aktualisieren.
6. Benötigte Apps, Services, Diagnostics, Drivers, Protocols und Subsystems einzeln mit
   Rollenflag und Komponentenname beziehen.
7. Die DevKit-Abhängigkeiten anschließend über dessen Setup installieren; die
   dortigen öffentlichen Quellkopien werden ebenfalls anonym bezogen.
8. Nur wenn in R4OSDev gepusht werden soll, Benutzername und Token in die
   lokale Credentials-Datei eintragen.

Der DevKit-Checkout ist nicht mit den dort installierten Quellkopien zu
verwechseln. Kanonische Quellen werden unter `Repositories/` bearbeitet;
installierte DevKit-Kopien sind kein Bearbeitungs- oder Pushziel.


Fehlerbehandlung
----------------

Credentials fehlen oder enthalten Platzhalter
    Nur Push und Repository-Erstellung sind betroffen. Unter Windows
    `Tools\Setup.bat`, unter Linux `./Tools/Setup.sh` ausfuehren und den
    Benutzer die lokale Datei vor dem Push ausfüllen lassen.
    Öffentliche Pulls und Setups funktionieren weiterhin anonym. Credentials
    nicht selbst auslesen oder ausgeben.

Repository steht nicht auf main
    Status und Branches prüfen. Nicht automatisch wechseln, wenn dadurch
    laufende Arbeit gefährdet wird. Github.ps1 arbeitet absichtlich nur auf
    main.

origin zeigt auf eine andere URL
    Remote mit git remote get-url origin prüfen. Die Ursache klären und nicht
    blind überschreiben. Das Script verweigert den Zugriff absichtlich.

Pull ist kein Fast-Forward
    git status und die lokale sowie entfernte Historie prüfen. Kein Force-Pull,
    kein Reset und kein automatischer Merge. Die Divergenz bewusst auflösen.

GitHub-Repository ist nicht erreichbar
    Bei Pull Zielname, Netzwerk und öffentliche Sichtbarkeit prüfen. Ein Pull
    verwendet keinen Token und legt kein fehlendes Remote-Repository an.

Repository kann nicht erstellt werden
    Der Token darf in R4OSDev vermutlich keine Repositories anlegen.
    Berechtigung durch den Benutzer korrigieren lassen oder das leere Zielrepo
    administrativ anlegen lassen und denselben Push erneut starten.

Komponentenname wird abgelehnt
    Exakten lokalen Verzeichnisnamen verwenden. Erlaubt sind ausschließlich
    Buchstaben, Ziffern und Bindestriche mit alphanumerischem Anfang.

Push enthält unerwartete Dateien
    Nicht pushen. Status und .gitignore des ausgewählten Einzelrepositories
    prüfen. Keine fremden Dateien löschen oder zurücksetzen. Beachten, dass
    der globale GitHub-Starter immer git add -A verwendet.

Push meldet keine neuen Dateien
    Dies ist kein Fehler. Das Script pusht anschließend trotzdem main. Prüfen,
    ob das richtige Zielrepository gewählt wurde und ob die Dateien ignoriert
    oder Teil eines anderen Einzelrepositories sind.

Sammel-Push endet mit Fehler
    Die getrennten Listen und die abschließende Restprüfung lesen. Erfolgreich
    gepushte Repositories nicht zurücksetzen. Nur die konkret fehlgeschlagenen
    Branch-, Remote-, Credential-, Commit- oder Pushprobleme beheben und den
    Sammel-Push erneut ausführen.


Grenzen des Scripts
-------------------

- Kein Sammel-Pull; Pulls bleiben gezielt und verwenden ausschließlich
  Fast-Forward.
- Keine Pull Requests, Reviews oder Freigaben im Browser.
- Keine Arbeit auf Feature-Branches.
- Kein Merge, Rebase, Reset, Clean oder Stash.
- Keine automatische Auflösung von Konflikten oder Divergenzen.
- Keine automatische Aktualisierung von Abhängigkeiten vor einem Build.
- Keine Veröffentlichung ignorierter Buildartefakte, Toolinstallationen oder
  Credentials.
- Neue zentrale Repositoryarten benötigen eine ausdrückliche Zuordnung in
  Tools/Github.ps1 und in der festen Sammelreihenfolge. Apps, Services,
  Diagnostics, Drivers, Protocols und Subsystems werden bereits generisch über
  Rollenverzeichnis und Komponentenname erkannt. Der Package-Server bleibt
  bewusst außerhalb dieses globalen Scripts.
